# Layer for Evaluation builds of GyroidOS

This layer makes some additional modification to mark
GyroidOS release builds as evaluation builds visible to the user.

Currently this is done by overwriting the motd.
